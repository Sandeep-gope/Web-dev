// Understanding the dynamic nature of the object

let obj = {
    age:22,
    wt:55,
    ht:5.5
};

console.log(obj);

// It is dynamic in nature because we can add data at the run time
// As we have done here in the obj.color section
obj.color = "black";
console.log(obj);